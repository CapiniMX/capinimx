import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.capinimx.musica',
  appName: 'La Aventura Musical',
  webDir: 'dist'
};

export default config;
